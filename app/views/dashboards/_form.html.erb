<%= form_with(model: dashboard, local: true) do |form| %>
<% if dashboard.errors.any? %>
<div id="error_explanation", style="font-size:20px; color:gray;" >
  <h2><%= pluralize(dashboard.errors.count, "error") %> prohibited this dashboard from being saved:</h2>

  <ul>
    <% dashboard.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div class="field">
  <%= form.label :title, style:"font-size:20px; font-weight: bold;" %>
  <%= form.text_field :title %>
</div>

<div class="field">
  <%= form.label :description, style:"font-size:20px; font-weight: bold;" %>
  <%= form.text_field :description %>
</div>

<div class="actions">
  <%= form.submit :class => 'btn btn-secondary btn-sm' %>
</div>
<% end %>